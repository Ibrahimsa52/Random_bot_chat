# Telegram Random Chat Bot 🤖

بوت تليجرام للشات العشوائي بين المستخدمين - Random 1-on-1 chat bot on Telegram

## 📋 الوصف

هذا بوت يتيح للمستخدمين الدخول في محادثات عشوائية مع أشخاص آخرين بشكل مجهول على تليجرام، شبيه بـ Omegle.

## ✨ المميزات

- 🔍 بحث عشوائي عن شريك محادثة
- 💬 نقل فوري للرسائل بين المستخدمين
- ❌ إنهاء المحادثة في أي وقت
- 🛡️ حماية من السبام
- 👮 لوحة تحكم للإدارة
- 📊 إحصائيات المستخدمين
- 🚫 نظام حظر وإبلاغ
- 📢 إرسال رسائل جماعية

## 🚀 التثبيت والإعداد

### المتطلبات الأساسية

- Python 3.9 أو أحدث
- حساب Firebase (مجاني)
- بوت Telegram

### خطوات التثبيت

1. **استنساخ المشروع**
```cmd
git clone <repo-url>
cd Bot
```

2. **تثبيت المكتبات**
```cmd
pip install -r requirements.txt
```

3. **إعداد Firebase**

   أ. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
   
   ب. أنشئ مشروع جديد أو استخدم مشروع موجود
   
   ج. فعّل Firestore Database:
      - من القائمة الجانبية، اختر "Firestore Database"
      - اضغط "Create database"
      - اختر "Start in production mode"
      - اختر الموقع الأقرب لك
   
   د. احصل على Service Account Key:
      - اذهب إلى Project Settings (⚙️) > Service Accounts
      - اضغط "Generate new private key"
      - سيتم تنزيل ملف JSON
      - أعد تسمية الملف إلى `serviceAccountKey.json`
      - ضعه في مجلد المشروع

4. **إعداد ملف البيئة**

   أنشئ ملف `.env` في المجلد الرئيسي:
   ```
   BOT_TOKEN=7465914668:AAFUrhh3iPhGPlXMfOCLeJkqg4R4l1KA_Dg
   ADMIN_IDS=1713792928
   FIREBASE_CRED_PATH=serviceAccountKey.json
   ```

5. **تشغيل البوت**
```cmd
python bot.py
```

## 📱 الأوامر المتاحة

### أوامر المستخدمين

| الأمر | الوصف |
|-------|------|
| `/start` | بدء استخدام البوت |
| `/search` | البحث عن شريك عشوائي |
| `/end` | إنهاء المحادثة الحالية |
| `/help` | عرض المساعدة |
| `/report` | الإبلاغ عن مستخدم |

### أوامر الإدارة

| الأمر | الوصف |
|-------|------|
| `/admin_stats` | عرض إحصائيات البوت |
| `/admin_block <user_id>` | حظر مستخدم |
| `/admin_unblock <user_id>` | إلغاء حظر مستخدم |
| `/admin_broadcast <message>` | إرسال رسالة جماعية |
| `/admin_reports` | عرض البلاغات |
| `/admin_chats` | عرض المحادثات النشطة |

## 🏗️ هيكل المشروع

```
Bot/
├── bot.py                  # الملف الرئيسي للبوت
├── config.py              # الإعدادات والمتغيرات
├── database.py            # طبقة قاعدة البيانات (Firebase)
├── messages.py            # قوالب الرسائل
├── admin.py               # أوامر الإدارة
├── spam_protection.py     # الحماية من السبام
├── requirements.txt       # المكتبات المطلوبة
├── .env                   # متغيرات البيئة (لا ترفعه على Git)
├── serviceAccountKey.json # مفتاح Firebase (لا ترفعه على Git)
└── README.md             # هذا الملف
```

## 🔒 الأمان

- ⚠️ **لا ترفع** ملفات `.env` أو `serviceAccountKey.json` على Git
- 🔑 التوكن ومعرفات المشرفين محفوظة في متغيرات البيئة
- 🛡️ حماية من السبام بتحديد عدد الرسائل في الدقيقة
- 🚫 نظام حظر تلقائي للمخالفين

## 📊 قاعدة البيانات

### Collections في Firestore

**users**
- `user_id`: معرف المستخدم
- `joined_at`: تاريخ الانضمام
- `blocked`: هل محظور؟
- `current_chat_with`: مع من يتحدث حالياً
- `total_chats`: عدد المحادثات الكلي
- `reports_count`: عدد البلاغات

**active_chats**
- `user1_id`: المستخدم الأول
- `user2_id`: المستخدم الثاني
- `started_at`: وقت البدء

**reports**
- `reporter_id`: المبلّغ
- `reported_id`: المبلّغ عنه
- `reason`: السبب
- `timestamp`: الوقت

**waiting_queue**
- قائمة المستخدمين المنتظرين للتوصيل

## 🌐 النشر والاستضافة

### تشغيل محلي
```cmd
python bot.py
```

### نشر على VPS
```bash
# تثبيت المكتبات
pip install -r requirements.txt

# تشغيل البوت في الخلفية
nohup python bot.py &
```

### نشر على Railway / Render
1. ارفع الكود على GitHub
2. اربط الريبو بـ Railway/Render
3. أضف متغيرات البيئة في لوحة التحكم
4. ارفع ملف `serviceAccountKey.json` كملف سري

## 🐛 استكشاف الأخطاء

**البوت لا يرد:**
- تأكد من التوكن صحيح
- تأكد من تشغيل البوت `python bot.py`

**خطأ Firebase:**
- تأكد من وجود `serviceAccountKey.json`
- تأكد من تفعيل Firestore في Firebase Console

**خطأ في الأوامر:**
- تأكد من كتابة الأمر بشكل صحيح
- جرب `/start` للبدء من جديد

## 📝 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

## 🤝 المساهمة

المساهمات مرحب بها! يمكنك فتح Issue أو Pull Request.

---

صُنع بـ ❤️ باستخدام Python و Firebase
