# 🔥 دليل إعداد Firebase للبوت

هذا الدليل يشرح خطوة بخطوة كيف تعمل مشروع Firebase وتحصل على ملف `serviceAccountKey.json`.

## الخطوات

### 1. إنشاء حساب Firebase (إذا لم يكن لديك)

1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. سجل دخول بحساب Google
3. اضغط "Get Started" إذا كانت أول مرة

### 2. إنشاء مشروع جديد

1. اضغط على **"Add Project"** أو **"إضافة مشروع"**
2. اكتب اسم المشروع (مثلاً: `telegram-random-chat`)
3. اضغط **Continue**
4. اختر إذا كنت تريد Google Analytics (اختياري - يمكن إلغاء التفعيل)
5. اضغط **Create Project**
6. انتظر حتى يتم إنشاء المشروع (10-30 ثانية)
7. اضغط **Continue** للدخول للمشروع

### 3. تفعيل Firestore Database

1. من القائمة الجانبية، اختر **"Firestore Database"**
2. اضغط على **"Create database"**
3. اختر موقع Database:
   - اختر **"Start in production mode"** (أكثر أماناً)
   - اضغط **Next**
4. اختر الموقع الجغرافي الأقرب لك (مثلاً: `eur3` لأوروبا أو `nam5` لأمريكا)
5. اضغط **Enable**
6. انتظر حتى يتم التفعيل

### 4. إعداد Security Rules (اختياري للحماية)

بعد تفعيل Firestore، اضغط على تبويب **"Rules"** وضع القواعد التالية:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false; // Only backend can access
    }
  }
}
```

اضغط **Publish** لحفظ القواعد.

### 5. الحصول على Service Account Key

1. اضغط على أيقونة **⚙️ (Settings)** بجانب "Project Overview"
2. اختر **"Project Settings"**
3. اذهب إلى تبويب **"Service accounts"**
4. تأكد أنك في **"Firebase Admin SDK"**
5. اختر اللغة **Python**
6. اضغط على زر **"Generate new private key"**
7. سيفتح لك نافذة تأكيد - اضغط **"Generate key"**
8. سيتم تنزيل ملف JSON تلقائياً (اسمه شبيه بـ `projectname-firebase-adminsdk-xxxxx.json`)

### 6. نقل الملف للمشروع

1. أعد تسمية الملف المحمّل إلى: **`serviceAccountKey.json`**
2. انسخ الملف إلى مجلد المشروع `d:\code\Bot\`
3. **مهم جداً**: تأكد أن الملف `.gitignore` يحتوي على `serviceAccountKey.json` حتى لا يتم رفعه على Git

### 7. التأكد من صحة الإعداد

ملف `.env` يجب أن يحتوي على:

```
BOT_TOKEN=7465914668:AAFUrhh3iPhGPlXMfOCLeJkqg4R4l1KA_Dg
ADMIN_IDS=1713792928
FIREBASE_CRED_PATH=serviceAccountKey.json
```

### 8. تشغيل البوت

الآن افتح Terminal وشغّل:

```cmd
cd d:\code\Bot
pip install -r requirements.txt
python bot.py
```

## 🔒 ملاحظات أمان مهمة

⚠️ **لا ترفع ملف `serviceAccountKey.json` على GitHub أو أي مكان عام!**

- هذا الملف يعطي وصول كامل لقاعدة البيانات
- لو تم نشره، أي شخص يقدر يعدل أو يحذف كل بياناتك
- دايماً تأكد أنه في `.gitignore`

## 🆓 الباقة المجانية

Firebase يوفر باقة مجانية كافية جداً:

- **50,000** عملية قراءة يومياً
- **20,000** عملية كتابة يومياً
- **1 GB** تخزين

البوت ده هيشتغل بشكل ممتاز على الباقة المجانية لآلاف المستخدمين.

## ❓ مشاكل شائعة

**خطأ: "`Cannot find module 'serviceAccountKey.json'`"**
- تأكد أن الملف موجود في نفس مجلد `bot.py`
- تأكد من اسم الملف صحيح تماماً

**خطأ: "`Permission denied`"**
- تأكد أنك عملت Enable لـ Firestore Database
- تأكد أن Service Account Key تم توليده بشكل صحيح

**خطأ: "`Invalid credentials`"**
- حمّل ملف Service Account Key جديد
- تأكد أن الملف غير تالف (افتحه بـ Notepad وشوف لو يحتوي JSON صحيح)

---

بالتوفيق! 🚀
